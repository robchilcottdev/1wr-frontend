import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-dock',
  imports:  [RouterLink],
  templateUrl: './dock.html'
})

export class Dock {
}
